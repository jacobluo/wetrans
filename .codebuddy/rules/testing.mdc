---
description: Verification commands and testing requirements for the native macOS SwiftPM app.
alwaysApply: true
enabled: true
provider:
---

# 测试与质量规则

> Canonical detailed rule: this file. Codex and Claude reach it through `AGENTS.md`.

## 项目类型

wetrans 是 SwiftPM-first 的原生 macOS 应用，不是 TypeScript / React / Playwright Web 项目。

当前工程入口：

- Package manifest: `Package.swift`
- App executable product: `wetrans`
- App target: `wetransApp`
- Library target: `wetrans`
- Unit test target: `wetransTests`

## 验证入口

优先使用仓库脚本：

- `scripts/setup`
  - 检查 Swift toolchain。
  - 执行 `swift package resolve`。
- `scripts/lint`
  - 执行 SwiftPM manifest sanity check。
  - 如果本机安装了 `swift-format`，执行 Swift 格式 lint；没有安装时明确跳过。
- `scripts/typecheck`
  - 执行 `swift build`。
- `scripts/test`
  - 执行 `swift test`。
- `scripts/e2e`
  - 执行默认 E2E 路径。
  - 先启动本地 Docker OpenSSH fixture 并运行 `RemoteFileSystemRealHostIntegrationTests`，验证 libssh2 SFTP key/password connect/list/upload/download。
  - 再执行 packaged app build/run smoke，并通过 native Accessibility runner 检查主窗口关键 UI 锚点。
  - 完整 UI 场景仍通过 `WETRANS_E2E_RUN_FULL=1` 显式开启。
- `scripts/verify`
  - 依次执行 `scripts/lint`、`scripts/typecheck`、`scripts/test`、`scripts/e2e`。

完成前优先运行：

```bash
scripts/verify
```

如果只改了很窄的逻辑，可以先跑聚焦测试，例如：

```bash
swift test --filter MainBrowserViewModelTests
swift test --filter TransferQueueTests
```

最终交付前仍应运行 `scripts/verify`，除非明确说明无法运行的阻塞原因。

## 测试要求

- 新生产行为必须有测试。
- Bug 修复必须有回归测试。
- 文档-only、纯样式且无行为变化、或用户明确批准的 spike 可以例外。
- 测试应验证用户可见行为或稳定接口，避免绑定无意义的内部实现细节。
- Mock 网络、SFTP、Keychain、文件系统、剪贴板、Finder reveal 等系统边界应放在协议边界。
- 不要 mock 正在被测试的行为本身。

## macOS / Swift 测试边界

单元测试覆盖率按行为和风险面管理：

- 新增或修改的纯函数、状态机、路径计算、任务队列、持久化 schema、错误映射和安全边界必须有单元测试。
- View model 的用户动作必须有测试，覆盖成功路径、禁用/拒绝路径和主要错误路径。
- SwiftUI 视图目前以 compile/render smoke tests 为主；涉及重要交互时，应优先把行为下沉到 view model 并测试。
- Keychain 等环境相关能力默认通过 fake/protocol 测试。
- 真实 libssh2 runtime 探针仍由环境变量显式开启：

```bash
WETRANS_RUN_LIBSSH2_REAL_PROBE=1 swift test --filter LibSSH2RuntimeRealProbeTests
```

真实 SFTP 主路径现在由 `RemoteFileSystemRealHostIntegrationTests` 默认覆盖，依赖提交的非 secret fixture 或 `WETRANS_SFTP_INTEGRATION_FILE` 本地覆盖。

## UI/E2E 策略

当前仓库有 committed packaged-app smoke path 和 native Accessibility runner。

当前 E2E 分为两层：

- 不要编造 Playwright、Chromium、Web E2E 覆盖。
- `scripts/e2e` 默认运行真实 SFTP connect/list/upload/download E2E。
- `scripts/e2e` 默认运行 packaged app build/run smoke，检查 `Connect Host`、`Local File Panel`、`Remote File Panel`、`Transfer Queue`。
- 用户可见核心流程仍应由 view model/unit tests 和 SwiftUI smoke tests 覆盖。

完整 UI 自动化仍是 opt-in：

- `scripts/verify` 必须继续包含 `scripts/e2e`。
- `WETRANS_E2E_RUN_FULL=1` 可运行 manual host、SSH Config host、transfer 等 UI 场景。
- 内测前默认路径至少覆盖：本地 Docker OpenSSH 连接、key/password 认证、浏览远端目录、单文件/多文件/目录上传、单文件/多文件/目录下载、启动 app、打开主窗口、队列区域可见。

## 完成标准

- 完成前运行与变更范围匹配的聚焦测试。
- 最终交付前运行 `scripts/verify`。
- 无法运行验证命令时，必须说明阻塞原因和用户可手动执行的命令。
